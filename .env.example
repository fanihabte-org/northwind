# Absolute host path used only for durable Bulk Query CSV result artifacts.
# Copy to .env and replace "your-user" before starting Docker Compose.
FAKEFORCE_EXPORTS_DIR=/Users/your-user/data_forge/exports
